{{- if .Values.backend.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "taipei-city-dashboard.fullname" . }}-backend
  labels:
    {{- include "taipei-city-dashboard.backend.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.backend.replicaCount }}
  {{- end }}
  strategy:
    type: {{ default "RollingUpdate" .Values.backend.updateStrategy.type }}
    {{- if and .Values.backend.updateStrategy.rollingUpdate (eq (default "RollingUpdate" .Values.backend.updateStrategy.type) "RollingUpdate") }}
    rollingUpdate:
      maxSurge: {{ .Values.backend.updateStrategy.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.backend.updateStrategy.rollingUpdate.maxUnavailable }}
    {{- end }}
  selector:
    matchLabels:
      {{- include "taipei-city-dashboard.backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "taipei-city-dashboard.backend.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "taipei-city-dashboard.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: backend
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ include "taipei-city-dashboard.image" (merge (dict "repository" .Values.backend.image.repository "tag" .Values.backend.image.tag) .) }}
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.backend.service.targetPort }}
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
          env:
            {{- range $key, $value := .Values.backend.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- if not .Values.backend.useSecretDbDashboardPort }}
            - name: DB_DASHBOARD_PORT
              value: {{ default 5432 .Values.backend.db.dashboard.port | quote }}
            {{- end }}
            - name: DB_DASHBOARD_SSLMODE
              value: {{ default "require" .Values.backend.db.dashboard.sslmode | quote }}
            {{- if not .Values.backend.useSecretDbManagerPort }}
            - name: DB_MANAGER_PORT
              value: {{ default 5432 .Values.backend.db.manager.port | quote }}
            {{- end }}
            - name: DB_MANAGER_SSLMODE
              value: {{ default "require" .Values.backend.db.manager.sslmode | quote }}
            {{- if not .Values.backend.useSecretRedisPort }}
            - name: REDIS_PORT
              value: {{ default 6379 .Values.backend.redis.port | quote }}
            {{- end }}
            {{- if not .Values.backend.useSecretRedisDb }}
            - name: REDIS_DB
              value: {{ default 0 .Values.backend.redis.db | quote }}
            {{- end }}
            # Database configuration from secrets (always)
            - name: DB_DASHBOARD_HOST
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: DB_DASHBOARD_HOST
            {{- if .Values.backend.useSecretDbDashboardPort }}
            - name: DB_DASHBOARD_PORT
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: DB_DASHBOARD_PORT
                  optional: true
            {{- end }}
            - name: DB_DASHBOARD_DBNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: DB_DASHBOARD_DBNAME
            - name: DB_DASHBOARD_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: DB_DASHBOARD_USER
            - name: DB_DASHBOARD_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: DB_DASHBOARD_PASSWORD
            # Manager DB (default to dashboard secrets if separate not provided)
            - name: DB_MANAGER_HOST
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: DB_MANAGER_HOST
                  optional: true
            {{- if .Values.backend.useSecretDbManagerPort }}
            - name: DB_MANAGER_PORT
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: DB_MANAGER_PORT
                  optional: true
            {{- end }}
            - name: DB_MANAGER_DBNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: DB_MANAGER_DBNAME
                  optional: true
            - name: DB_MANAGER_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: DB_MANAGER_USER
                  optional: true
            - name: DB_MANAGER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: DB_MANAGER_PASSWORD
                  optional: true
            # Redis configuration from secrets (always)
            - name: REDIS_HOST
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: REDIS_HOST
            {{- if .Values.backend.useSecretRedisPort }}
            - name: REDIS_PORT
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: REDIS_PORT
                  optional: true
            {{- end }}
            {{- if .Values.backend.useSecretRedisDb }}
            - name: REDIS_DB
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: REDIS_DB
                  optional: true
            {{- end }}
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: REDIS_PASSWORD
                  optional: true
            {{- range .Values.backend.secretEnv }}
            {{- if not (has .name (list "DB_DASHBOARD_HOST" "DB_DASHBOARD_USER" "DB_DASHBOARD_PASSWORD" "DB_DASHBOARD_DBNAME" "DB_DASHBOARD_PORT" "DB_MANAGER_HOST" "DB_MANAGER_USER" "DB_MANAGER_PASSWORD" "DB_MANAGER_DBNAME" "DB_MANAGER_PORT" "REDIS_HOST" "REDIS_PORT" "REDIS_PASSWORD" "REDIS_DB")) }}
            - name: {{ .name }}
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else }}
              valueFrom:
                secretKeyRef:
                  name: {{ include "taipei-city-dashboard.fullname" $ }}-secrets
                  key: {{ default .name .secretKey }}
                  {{- if .optional }}
                  optional: true
                  {{- end }}
              {{- end }}
            {{- end }}
            {{- end }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
