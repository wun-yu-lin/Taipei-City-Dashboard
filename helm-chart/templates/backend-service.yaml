{{- if .Values.backend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "taipei-city-dashboard.fullname" . }}-backend
  labels:
    {{- include "taipei-city-dashboard.backend.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.backend.service.annotations }}
    {{- range $k, $v := . }}
    {{ $k }}: {{ $v | quote }}
    {{- end }}
    {{- end }}
spec:
  type: {{ .Values.backend.service.type }}
  {{- with .Values.backend.service.loadBalancerIP }}
  loadBalancerIP: {{ . }}
  {{- end }}
  {{- with .Values.backend.service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taipei-city-dashboard.backend.selectorLabels" . | nindent 4 }}
{{- end }}
